// Partially applied function
// If a function literal consists of one statement
// and takes a single argument, you need not explicitly name
// and specify the argument.

// args.foreach(args => println(args))

args.foreach(println)
