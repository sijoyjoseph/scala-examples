// for loop in scala.

for (arg <- args)
  println(arg)

// here arg is val not var. It cannot be reassigned.
// Instead for each values of the args a new arg val
// will be created and the initialized and the body
// of the for loop will be executed.
